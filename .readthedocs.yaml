# .readthedocs.yaml
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    pre_install:
      - pip install uv
    install:
      - uv pip install -r docs/requirements.txt
      - uv pip install --no-deps .

sphinx:
  configuration: docs/source/conf.py